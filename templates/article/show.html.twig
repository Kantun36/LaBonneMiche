{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
    <h1>Article</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ article.Titre }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ article.Contenu }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>
                </td>
            <tr>
                    {% for paragraphe in article.paragraphes %}
                <th>Paragraphe numero {{ paragraphe.numeroParagraphe }}</th>
                <td>
                        <div class="container">
                            <div class="title h3 ">
                                {{ paragraphe.titreParagraphe }}
                            </div>
                            <div class="text">
                                {{ paragraphe.contenuParagraphe }}
                            </div>
                        </div>
                </td>
            </tr>
                    {% endfor %}


        </tbody>
    </table>



    <a href="{{ path('article_index') }}">back to list</a>

    <a href="{{ path('article_edit', {'id': article.id}) }}">edit</a>

    {{ include('article/_delete_form.html.twig') }}
{% endblock %}
